# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Django
db.sqlite3
media/
staticfiles/

# Node
node_modules/
dist/
.vite/
coverage/
playwright-report/
test-results/
# Build artefacts: fetch-fallback.ts regenerates all three on every build, and
# the sitemap needs the real deploy URL, which is only known at build time.
/frontend/src/api/fallback.json
/frontend/public/sitemap.xml
/frontend/public/robots.txt

# Env & secrets — never commit these
.env
.env.local
.env.*.local

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
*.jsonl
